/**
 * Placeholder de los tipos de la base de datos.
 *
 * Este archivo se va a regenerar desde el esquema real de Supabase con:
 *   npx supabase gen types typescript --project-id <PROJECT_ID> > src/types/database.ts
 *
 * Hasta entonces `Database` no declara ninguna tabla.
 */
export type Database = Record<string, never>;
